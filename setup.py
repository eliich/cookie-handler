from setuptools import setup, find_packages

setup(
    name='cookie_handler',
    version='0.1.0',
    packages=find_packages(),
    include_package_data=True,
    description='A Python package to handle cookies',
    author='eliich',
    author_email='example@example.com',  # Placeholder email
    url='https://github.com/eliich/cookie-handler',
    install_requires=[],  # No dependencies
)
